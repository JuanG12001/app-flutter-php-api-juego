name: app
description: Un nuevo proyecto Flutter.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  flutter_svg: ^1.0.0
  flutter_spinkit: ^5.0.0
  http: ^0.13.3
  fluttertoast: ^8.0.8
  flutter_facebook_auth: ^3.5.0
  google_sign_in: ^6.1.4
  flutter_keyboard_visibility: ^5.4.1
  provider: ^6.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
